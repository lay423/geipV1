<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTDMapper3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="hello.geip.mapper.BlueTeamMapper">
    <select id="getBlueTeamList" resultType="hello.geip.bean.CompetitionVo">
        SELECT lol_nick
        FROM competition a
        WHERE a.competition_name
        IN (SELECT b.competition_name FROM competition b WHERE lol_nick = 'kdk') and team = 'blue'
    </select>
</mapper>